# Spring Settings
spring.main.banner-mode=${SPRING_BANNER_MODE}

# DB Settings
spring.datasource.url=${DB_DRIVER}:${DB_PRODUCT}://${DB_HOST}:${DB_PORT}/${DB_NAME}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.hibernate.ddl-auto=${DB_DDL_AUTO}